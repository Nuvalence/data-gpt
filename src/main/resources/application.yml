spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://data-gpt-postgres:5432/datagpt
    username: root
    password: root
  liquibase:
    enabled: true
tables: film,category,film_category,actor,film_actor,inventory,rental,payment
